#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jshashes/bin/hashes" "$@"
  ret=$?
else 
  node  "$basedir/../jshashes/bin/hashes" "$@"
  ret=$?
fi
exit $ret
